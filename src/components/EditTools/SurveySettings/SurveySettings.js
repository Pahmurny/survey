import React from 'react';

const SurveySettings = ({ className }) => (
  <div className={`${className} SurveySettings`}>
    <div className="SurveySettings-header">Survey Settings</div>
    <div className="SurveySettings-content"> Some control elements here</div>
  </div>
);

export default SurveySettings;
